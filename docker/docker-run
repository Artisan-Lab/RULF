#! /usr/bin/env bash

set -e

VERSION="v0.2"
#readonly DOCKER_USER="${USER:-root}"
readonly DOCKER_USER="jjf"
readonly PWD=`pwd`
docker run -it --privileged=true -v ~/RULF:/home/${DOCKER_USER}/Fuzzing-Target-Generator -v ~/Fuzzing-Scripts:/home/${DOCKER_USER}/Scripts --rm fuzz-target-generator:$VERSION
